# # string methods:
# # --------------------------------
# # len() : returns the length of the string
# # lower() : returns the lowercase of the string
# # upper() : returns the uppercase of the string
# # title() : returns the title of the string
# # capitalize() : returns the capitalized of the string
# # strip() : returns the stripped of the string
# # replace() : returns the replaced string
# # find() : returns the index of the first occurrence of the substring
# # startswith() : returns True if the string starts with the substring
# # endswith() : returns True if the string ends with the substring
# # split() : returns the list of words in the string
# # join() : returns the joined string
# # isalpha() : returns True if the string contains only alphabets
# # isdigit() : returns True if the string contains only digits
# # isalnum() : returns True if the string contains only alphabets and digits
# # zfill() : returns the zero filled string
# # center(12,"*") : returns the centered string with the width of 12 and the character "*"
# # casefold() : returns the casefolded string
# # isspace() : returns True if the string contains only spaces
# # isupper() : returns True if the string contains only uppercase letters
# # islower() : returns True if the string contains only lowercase letters
# # istitle() : returns True if the string contains only titlecase letters
# # isprintable() : returns True if the string contains only printable characters
# # isidentifier() : returns True if the string contains only identifier characters
# # isdecimal() : returns True if the string contains only decimal characters
# # isnumeric() : returns True if the string contains only numeric characters


# # slice methods:
# # --------------------------------
# # slice(start, end, step) : returns the sliced string
# # slice(start, end) : returns the sliced string
# # slice(start) : returns the sliced string
# # slice(end) : returns the sliced string
# # slice(start, end, step) : returns the sliced string
# # slice(start, end, step) : returns the sliced string
# # slice(start, end, step) : returns the sliced string
# # slice(start, end, step) : returns the sliced string


# # list methods:
# # --------------------------------
# # append(): adds an element to the end of the list
# # clear(): removes all elements from the list
# # copy(): returns a copy of the list
# # count(): returns the number of elements with the specified value
# # extend(): adds the elements of a list (or any iterable), to the end of the current list
# # index(): returns the index of the first element with the specified value
# # insert(): adds an element at the specified position
# # pop(): removes the element at the specified position
# # remove(): removes the first item with the specified value
# # reverse(): reverses the order of the list
# # sort(): sorts the list
# # del : keyword for deleting the whole list


# # tuple methods:
# # -----------------
# # count(): returns the number of times a specified value occurs in a tuple
# # index(): searches the tuple for a specified value and return the position of where it was found
# # len()  : find the length of the tuple or list


# # set methods:
# # ----------------

# add()	 :  	Adds an element to the set
# clear()	:  	Removes all the elements from the set
# copy()	: 	Returns a copy of the set
# difference():	-	Returns a set containing the difference between two or more sets
# difference_update():	-=	Removes the items in this set that are also included in another, specified set
# discard()	: 	Remove the specified item
# intersection():	&	Returns a set, that is the intersection of two other sets
# intersection_update():	&=	Removes the items in this set that are not present in other, specified set(s)
# isdisjoint():	 	Returns whether two sets have a intersection or not
# issubset()	:<=	Returns True if all items of this set is present in another set
#  	<	Returns True if all items of this set is present in another, larger set
# issuperset():	>=	Returns True if all items of another set is present in this set
#  	>	Returns True if all items of another, smaller set is present in this set
# pop()	: 	Removes an element from the set
# remove()	: 	Removes the specified element
# symmetric_difference():	^	Returns a set with the symmetric differences of two sets
# symmetric_difference_update():	^=	Inserts the symmetric differences from this set and another
# union():	|	Return a set containing the union of sets
# update():	|=	Update the set with the union of this set and others


# dictionary methods:
# ----------------------


# clear()	     : Removes all the elements from the dictionary
# copy()	     : Returns a copy of the dictionary
# fromkeys()	 : Returns a dictionary with the specified keys and value
# get()	       : Returns the value of the specified key
# items()	     : Returns a list containing a tuple for each key value pair
# keys()	     : Returns a list containing the dictionary's keys
# pop()	       : Removes the element with the specified key
# popitem()	   : Removes the last inserted key-value pair
# setdefault() : Returns the value of the specified key. If the key does not exist: insert the key, with the specified value
# update()	   : Updates the dictionary with the specified key-value pairs
# values()	   : Returns a list of all the values in the dictionary
